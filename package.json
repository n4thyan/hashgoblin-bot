{
  "name": "hashgoblin-bot",
  "version": "1.2.0",
  "description": "Discord economy bot with Glory currency, SHA-256 proof receipts, exact odds, global leaderboards, greetings, auto-roles, admin audit tools and deployment-safe Discord setup templates.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check src/index.js && node --check src/deploy-commands.js && find src -name '*.js' -not -path 'src/index.js' -not -path 'src/deploy-commands.js' -print0 | xargs -0 -n1 node --check",
    "test": "node tools/selftest.js",
    "smoke": "node tools/smoke-test.js",
    "release-check": "node tools/release-check.js",
    "preflight": "npm run check && npm run test && npm run smoke && npm run release-check"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "discord.js": "^14.26.4",
    "dotenv": "^16.5.0"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "license": "MIT"
}
